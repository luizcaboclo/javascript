let altura, base, area

altura = prompt("fale o valor da altura")
base = prompt("fale o valor para base")
if(altura > 0 && base > 0){
    alert("a área do triângulo é" + area)
}else{
    alert(" ou a base ou a altura do triâgulo não pode ser negativo")
}